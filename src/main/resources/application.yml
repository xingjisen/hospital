server:
  # 端口号
  port: 8080
spring:
  # 应用名称
  application:
    name: hospital
  # 数据源
  datasource:
    # 主库数据源
    driver_class_name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://101.43.167.87:23306/reggie?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
    username: root
    password: 123456
mybatis-plus:
  # 配置文件映射
  #  config-location: classpath:/mybatis-config.xml
  # xml映射文件地址
  #  mapper-locations: classpath:/mapper/**.xml
  # 指定别名包扫描路径，xml文件可以直接使用全类名
  #  type-aliases-package: com.your.domain

  configuration:
    # 数据库驼峰方式映射
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    # 全局配置
    db-config:
      # 数据库配置
      id-type: auto